services:
  backend:
    build:
      context: backend
    container_name: back
    ports:
      - "9000:9000"
    volumes:
      - ./backend:/transcendence
  frontend:
    build:
      context: frontend
    container_name: front
    volumes:
      - ./frontend:/transcendence
volumes:
  backend:
  frontend:
